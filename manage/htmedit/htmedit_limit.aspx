<%
/*if(Session["admin_id"] == null)
{
	Session.Abandon();
	Response.Write("<script>alert('登录超时，请重新登录！'); window.close();</script>");
	return;
}

if(Session["admin_id"].ToString() == "")
{
	Session.Abandon();
	Response.Write("<script>alert('登录超时，请重新登录！'); window.close();</script>");
	return;
}

if(pub_limit != "")
{
	if(Session["admin_limit"].ToString().IndexOf(pub_limit) < 0)
	{
		Session.Abandon();
		Response.Write("<script>alert('登录超时，请重新登录！'); window.close();</script>");
		return;
	}
}*/
%>